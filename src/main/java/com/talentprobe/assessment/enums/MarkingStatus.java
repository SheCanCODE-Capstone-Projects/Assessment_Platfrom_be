package com.talentprobe.assessment.enums;

public enum MarkingStatus {
    PENDING,   // submission just received, not yet graded
    PASSED,    // candidate passed this question
    FAILED     // candidate failed this question
}
